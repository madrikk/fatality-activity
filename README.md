# Fatality — guild activity

Auto-generated by [guildtrack]. The dashboard is `index.html`, published to
GitHub Pages and regenerated after every scan (~15 min).

`data/` mirrors the raw scan log so this doubles as an offsite backup.
History is intentionally squashed to a single commit: the page is a regenerated
artifact, and `data/observations.jsonl` already contains the full member history.
